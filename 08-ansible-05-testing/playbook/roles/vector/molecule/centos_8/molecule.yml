---
dependency:
  name: galaxy
driver:
  name: delegated
platforms:
  - name: instance
    image: quay.io/centos/centos:8
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
